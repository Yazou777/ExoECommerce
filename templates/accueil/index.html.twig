{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}

<h1> Catégories </h1>
      {% for categorie in categories %}
      <a href="{{ path('app_guitares_basses') }}">
      <div>
            {{ categorie.nom }}
      </div>
       <div>
           <img src="/images/{{ categorie.image }}"> 
      </div>
      </a>
        {% endfor %}

{% endblock %}
